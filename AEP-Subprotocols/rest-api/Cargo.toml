[package]
name = "aep-subprotocol-rest-api"
description = "AEP REST API subprotocol - endpoint call validation"
version.workspace = true
edition = "2021"
license = "Apache-2.0"

[dependencies]
aep-subprotocol-core = { path = "../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"