AEP 2.8 reference library remediation.

This plan was checked against Gitea repository thePM001/NLA-AEP-v2.8-open-source on branch main. Gitea remains the only official source of truth. The tree is a reference protocol library plus an instruction manual rather than a finished product that must already cover every surface, which README already states so the work is to make layout, versions, clocks and claims match that stance then raise kernel library quality. The umbrella ticket is AEP28-ENV-072. Child tickets AEP28-ENV-073 through AEP28-ENV-081 are the coding close units.

On Gitea main the pulse crate is a workspace member while leftover instruction-law crates are directories rather than workspace members. The Python SDK folder exists as source-only packages. dynAEP is a workspace member and lattice defaults use the AEP data dir. Poisoned locks already have a deny reply. Production lock expect calls in docking.rs sit under tests. The notes still hold where Gitea copy disagrees with the code or where the tree still looks like three products in one folder.

Keep Base Node as the kernel example with envelope, live-entry, evaluation-chain and lattice-channels as protocol pieces. Keep CAW as an optional execution companion rather than the protocol. Keep SDKs as thin clients and domain subprotocols as attach examples. Keep EPSCOM writing walls as protocol law while BIOSECURITY.md stays access policy and Gitea stays the write surface. Do not treat GitHub language bytes as architecture or promote scanner crates back into runtime members. Do not remove EPSCOM. Do not build a drone or ML kernel to satisfy headline copy. Do not add root GitHub Actions as theatre. Do not claim a language client is a native sealer when it shells out to aep-lattice-log.

Three products in one tree is applicable as layout and copy rather than as an emergency extract of CAW. The kernel is Rust while CAW is a Go execution box, Composer Lite is a WASM canvas and SDKs are clients. README still lists caw-framework under protocol components in the canonical layout table. Draw four rows for kernel, protocol, execution companion and clients. Relabel the CAW framework folder so it is not a protocol component. Keep CAW in the repo as a companion example if the operator wants it vendored. Stop calling the whole tree one hyperlattice because the hyperlattice is the wrap around a system while the repo is a library that shows how to attach that wrap. AEP28-ENV-072 must land a source_invariants or library-layer-count fail if Canonical layout still names caw-framework as a protocol component.

CI-gate crates as architecture is applicable as leftover directories rather than as runtime. Double-check found 41 leftover HAS_CRATE directories under AEP-Components that are not workspace members. Live enforcement for those laws already runs inside aep-base-node source_invariants folded_slogan_ci_gates. Relocate the duplicate directories to retired-archive/instruction-crates with an INVENTORY.gaplune. Keep companion folders that have no crate. Do not add leftover crates to workspace members. Ticket AEP28-ENV-073.

Headline timing invariants stay applicable because the stale no-crate part is done while the remaining defect is two clocks. The pulse clock is compiled freeze-at-seal with PULSE_MS 1000, MAX_DRIFT_MS 50 and MAX_AGE_MS 5000. The wire freshness clock is frame_is_fresh on sent_at_unix with MAX_FRAME_AGE_SECS 300 and MAX_FRAME_FUTURE_SKEW_SECS 60 still local in docking.rs. README says a capsule older than five seconds fails which is pulse age after freeze. The 300 second wire check is a different mechanism and is not named in the kernel pulse section. Compile both clocks in the pulse crate. Pulse crate tests fail if docking.rs reintroduces local magic numbers. Name them in README as pulse hold versus wire sent_at freshness and say the wire window is wider because it covers transit before open. Do not set MAX_DRIFT_MS to the pulse length. Ticket AEP28-ENV-074.

Version and leftover surfaces stay applicable. Closed ticket AEP28-ENV-029 bound banner, workspace and CHANNEL_VERSION at 2.8.0. README later drifted to 2.8.5 so source_invariants version_ssot must fail today. Public heading is 2.8.5 so the complete solve is one product string 2.8.5 across README, workspace, CHANNEL_VERSION, config loader, aep-lattice-log, subprotocol crates and SDK package versions. Schema Builder TypeScript exists and its own README says it is not wired to the CCA agent or Docker so keep that honesty in the main feature table. Drop compliance doc lines that treat it as a live Art. 9 or Art. 15 control. Archive trust-rings to retired-archive/retired/trust-rings. Ticket AEP28-ENV-075.

EPSCOM writing walls beside security walls is applicable as classification rather than as removal. Writing law is part of this protocol. Putting em dash, en dash, double hyphen and Oxford comma in the same Admit pass as replay and KEM is intentional here. The reviewer confusion is real because a writing deny can look like a security event. Keep EPSCOM at priority 255. Tag DenyReport closed walls with a class of writing versus security versus temporal versus capability plus poison and structural. The instruction manual should say writing walls govern output shape and are not transport security. Ticket AEP28-ENV-076.

One kernel for every surface is applicable as copy rather than as new domain kernels. aep-live-entry is a governed scene graph with z-bands, CP ids and Apply after Admit. Subprotocol crates exist as validators. That is the right size for a reference library. The weakness is README language about every surface plus drones, robots and ML pipelines. Say one kernel sequence with domain subprotocols as attach examples and live-entry as the worked scene. Keep commerce, IaC, events and REST subprotocols as instruction crates. Do not add a drone runtime. Ticket AEP28-ENV-077 covers this copy plus the SDK table.

Process versus the conformance claim is applicable with Gitea as the factory. There is no root GitHub workflow directory. CAW carries its own Actions. The mismatch is advertising CC-01 through CC-15 and CI gates as if the public GitHub snapshot ran them. Document the official run sequence as cargo test for aep-base-node lib, source_invariants, the gate-aep28-env scripts, the conformance runner and the Docker image. State that GitHub is a public mirror rather than the CI host. Dockerfile comment says no npm in the runtime image while the runtime stage still uses npm so the comment or the stage must match. Do not add root GitHub Actions unless the operator asks for a public mirror workflow. Ticket AEP28-ENV-078.

The SDK matrix is partially stale and still applicable as a maturity table. AEP-SDKs README labels every language Operational. TypeScript aep-protocol is a thin client. TypeScript dynAEP stays a standalone component rather than product Admit. Python is source-only fail-closed lattice client with no pip registry. Elixir and Go shell out to aep-lattice-log rather than native sealers. Astro, Vue and html-css are stubs or tokens. Publish one table with native sealer, thin client, source-only thin client, placeholder and not product Admit. Same ticket AEP28-ENV-077.

BIOSECURITY tone is not a code defect. Access policy stays. The engineering threat model stays under retired-archive security docs. Add a one-line pointer so a reviewer can read transport and crypto without treating access policy as the threat model. Fold that copy into AEP28-ENV-072 with no separate crate.

Kernel library quality stays in scope because a reference library still needs a readable kernel. docking.rs is about 2469 lines with many duties so split modules for wire freshness, pulse queue, rate limit, serve and apply while behavior stays. Ticket AEP28-ENV-079. Production mutex poison already returns a deny via lock_or_deny. Remaining lock expect calls sit under tests. Daemon stop now refuses new dock connections, joins dock tasks, unlinks sockets and closes SQLite. Ticket AEP28-ENV-080 is closed. Kernel crates should use thiserror rather than Result with String. Ticket AEP28-ENV-081. Lab tmp defaults are already off tmp so keep the guards and add no new work unless a default returns. Dual TypeScript Admit is already denied in changelog so do not add a second Admit.

Scanner-style tests that grep for forbidden strings are allowed as instruction law. They must not look like runtime components. Docker is already closer to a runnable reference than GitHub process so keep Docker as the run example and point the instruction manual at it.

Wave 1 is instruction honesty: AEP28-ENV-072 layout map, AEP28-ENV-074 two clocks, AEP28-ENV-075 one version string plus leftover Trust Rings, AEP28-ENV-077 surface copy plus SDK maturity table and AEP28-ENV-078 official test and gate run sequence. Wave 2 relocates leftover instruction-law crate directories as AEP28-ENV-073 after AEP28-ENV-075 archives Trust Rings. Wave 3 is kernel library quality: AEP28-ENV-076 DenyReport wall class in parallel with AEP28-ENV-080 dock drain, then AEP28-ENV-079 split docking.rs, then AEP28-ENV-081 kernel thiserror.

Non-goals are extracting CAW into its own product unless the operator orders a split, implementing every advertised surface, removing EPSCOM or writing walls, making GitHub the source of truth, treating TypeScript processEvent as product Admit and re-adding gate crates to workspace members.

Acceptance for this plan is that this file is on Gitea main with AEP28-ENV-072 open and pointing here and with child tickets AEP28-ENV-073 through AEP28-ENV-081 on Gitea main. There is no coding close on AEP28-ENV-072 until Wave 1 copy and layout land.
