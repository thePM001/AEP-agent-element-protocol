name: cluster
spec_ref: §7.3 CLUSTER, Appendix B maintenance
description: |
  CLUSTER foo - re-clusters a table. Maps to maintenance. Sample policy:
  implicit deny.
sql: "CLUSTER foo"
expected_classification:
  - raw_verb: CLUSTER
    primary_group: maintenance
    effects:
      - group: maintenance
expected_decision_under_sample_policy:
  verb: deny
