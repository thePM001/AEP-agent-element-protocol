name: drop-index
spec_ref: §7.3 DROP INDEX
description: DROP INDEX → schema_destroy(drop_index).
sql: "DROP INDEX idx_a"
expected_classification:
  - raw_verb: DROP_INDEX
    primary_group: schema_destroy
    primary_subtype: drop_index
    effects:
      - group: schema_destroy
        subtype: drop_index
        objects:
          - { kind: index, name: idx_a }
expected_decision_under_sample_policy:
  verb: deny
  rule_name: app-deny-mutations
