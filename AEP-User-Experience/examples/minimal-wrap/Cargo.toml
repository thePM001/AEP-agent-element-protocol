[package]
name = "aep-minimal-wrap"
version = "2.8.6"
edition = "2021"
license = "Apache-2.0"
description = "AEP 2.8.6 minimal wrap. Seals a payload, pulses, collects every wall and prints a deny report."

[workspace]

[dependencies]
aep-base-node = { path = "../../../AEP-Base-Node/crate" }
aep-lattice-crypto = { path = "../../../AEP-Components/lattice-crypto/crate" }
