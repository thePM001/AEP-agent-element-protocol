[workspace]

[package]
name = "aep-sdk"
version = "2.8.0"
edition = "2021"
description = "AEP 2.8 Rust SDK - lattice-gated client (no crates.io publish required for local use)"

[dependencies]
serde_json = "1"

[dev-dependencies]
serde_json = "1"