# AEP Rust SDK

Lattice-gated client crate (`build_lattice_frame`, `lattice_gated_fetch_url`).

```bash
cd AEP-SDKs/rust && cargo test
```

Standalone workspace (not part of repo root Cargo workspace).