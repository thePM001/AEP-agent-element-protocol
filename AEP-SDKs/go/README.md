# AEP Go SDK

Lattice-gated client (`BuildLatticeFrame`, `LatticeGatedFetch`). No module registry publish required.

```bash
cd AEP-SDKs/go && go test ./...
```

Requires `aep-lattice-log` on PATH (build via `cargo build --bin aep-lattice-log` in `AEP-Base-Node`).