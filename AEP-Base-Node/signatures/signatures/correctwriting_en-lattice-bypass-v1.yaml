signature_version: "1"
id: correctwriting_en-lattice-bypass-v1
authority: CORRECTWRITING_EN
name: Lattice channel bypass attempt
description: Detect raw HTTP/fetch calls that circumvent lattice-gated transport and UCB egress.
severity: critical
category: lattice
enabled: true
detection:
  type: regex
  case_insensitive: true
  patterns:
    - "fetch\\(['\"]https?://"
    - "curl\\s+(-[kL]*\\s+)*https?://"
    - "axios\\.(get|post|put|delete)\\("
    - "bypass.*lattice"
    - "skip.*ucb"
response:
  action: block
  dock: validation_engine
metadata:
  authored_by: CORRECTWRITING_EN
  revision: "2026-06-23"