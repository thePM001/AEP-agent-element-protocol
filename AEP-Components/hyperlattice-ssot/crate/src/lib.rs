// @PAD: gaplune-creation-pad via gaplune-pad-transform encode
// @GCDE: gaplune.policy.v1
// crate: aep-hyperlattice-ssot
// Generated by GAPLUNE Creation PAD ( zero-LLM, Rust-only )
// tokens: 0

use std::fs;
use std::path::PathBuf;

pub fn canonical_dir() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("../../dynAEP/bridge/hyperlattice")
}

pub fn replica_dir() -> PathBuf {
    PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("../../../AEP-SDKs/typescript/dynaep/src/hyperlattice")
}

pub fn assert_identical(name: &str) {
    let a = canonical_dir().join(name);
    let b = replica_dir().join(name);
    let left = fs::read_to_string(&a).unwrap_or_else(|e| panic!("read {}: {e}", a.display()));
    let right = fs::read_to_string(&b).unwrap_or_else(|e| panic!("read {}: {e}", b.display()));
    assert_eq!(left, right, "SSOT drift {name}");
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn hyperlattice_filter_matches() {
        assert_identical("HyperlatticeFilter.ts");
    }

    #[test]
    fn lattice_policy_evaluator_matches() {
        assert_identical("LatticePolicyEvaluator.ts");
    }

    #[test]
    fn compile_lattice_walls_matches() {
        assert_identical("compileLatticeWalls.ts");
    }

    #[test]
    fn compile_channel_order_walls_matches() {
        assert_identical("compileChannelOrderWalls.ts");
    }

    #[test]
    fn compile_temporal_walls_matches() {
        assert_identical("compileTemporalWalls.ts");
    }
}

