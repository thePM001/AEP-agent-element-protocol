// @PAD: gaplune-creation-pad via gaplune-pad-transform encode
// @GCDE: gaplune-decode hmac-sha256:b478d503a6842ab07413e7268f7c2ef74fb5f5e348059a3dcfd911e126da8cd9
// crate: aep-admit-temporal-bounds
// Generated by GAPLUNE Creation PAD ( zero-LLM, Rust-only )
// tokens: 0
// AEP28-ENV-005: Fold temporal bounds into Admit walls.

pub mod compile;
pub mod parse;
pub mod gate;

pub use compile::{
    causal_parent_wall_id, compile_temporal_walls, compile_temporal_warns, fold_temporal_into_admit,
    TemporalCompileInput, DEFAULT_MAX_DRIFT_MS, DEFAULT_MAX_FUTURE_MS, DEFAULT_MAX_STALENESS_MS,
    WALL_TEMPORAL_CAUSAL_PARENT, WALL_TEMPORAL_DRIFT, WALL_TEMPORAL_FUTURE, WALL_TEMPORAL_STALE,
};
pub use gate::{default_filter_ts, filter_folds_temporal, run_gate};
pub use parse::{parse_extra_walls, parse_temporal_from_text};

use aep_admit::AdmitWall;
use compile::{compile_temporal_walls as compile_walls_fn, compile_temporal_warns as compile_warns_fn};
use parse::parse_temporal_from_text as parse_fn;

// @PAD: gaplune-creation-pad emit ( zero-LLM )
// HVVCAS: compile_temporal_walls domain:admit type:service
pub mod compile_temporal_walls {
    use super::{compile_walls_fn, parse_fn, AdmitWall};

    pub struct CompileTemporalWalls {
        pub temporal: String,
        pub walls: Vec<AdmitWall>,
    }

    impl CompileTemporalWalls {
        pub fn new() -> Self {
            Self {
                temporal: String::new(),
                walls: Vec::new(),
            }
        }

        /// Compile live temporal denies clock skew and causal parent missing into Admit walls for one collect-all pass
        pub fn process(&mut self) -> anyhow::Result<()> {
            let input = parse_fn(&self.temporal);
            self.walls = compile_walls_fn(&input);
            Ok(())
        }
    }
}

// @PAD: gaplune-creation-pad emit ( zero-LLM )
// HVVCAS: compile_temporal_warns domain:admit type:service
pub mod compile_temporal_warns {
    use super::{compile_warns_fn, parse_fn};

    pub struct CompileTemporalWarns {
        pub temporal: String,
        pub warns: Vec<String>,
    }

    impl CompileTemporalWarns {
        pub fn new() -> Self {
            Self {
                temporal: String::new(),
                warns: Vec::new(),
            }
        }

        /// Emit soft temporal warns without closing Admit walls
        pub fn process(&mut self) -> anyhow::Result<()> {
            let input = parse_fn(&self.temporal);
            self.warns = compile_warns_fn(&input);
            Ok(())
        }
    }
}

// @PAD: gaplune-creation-pad emit ( zero-LLM )
// HVVCAS: fold_temporal_into_admit domain:admit type:service
pub mod fold_temporal_into_admit {
    use super::compile::fold_temporal_into_admit as fold_fn;
    use super::parse::{parse_extra_walls, parse_temporal_from_text};
    use aep_admit::AdmitResult;

    pub struct FoldTemporalIntoAdmit {
        pub extra: String,
        pub result: Option<AdmitResult>,
    }

    impl FoldTemporalIntoAdmit {
        pub fn new() -> Self {
            Self {
                extra: String::new(),
                result: None,
            }
        }

        /// Fold temporal walls plus extras into one Admit collect-all pass
        pub fn process(&mut self) -> anyhow::Result<()> {
            let temporal = parse_temporal_from_text(&self.extra);
            let extra = parse_extra_walls(&self.extra);
            self.result = Some(fold_fn(&temporal, &extra));
            Ok(())
        }
    }
}
