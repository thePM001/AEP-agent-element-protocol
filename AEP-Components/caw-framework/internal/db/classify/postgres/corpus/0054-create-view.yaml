name: create-view
spec_ref: §7.3 CREATE VIEW
description: CREATE VIEW maps to schema_create(create_view).
sql: "CREATE VIEW v AS SELECT 1"
expected_classification:
  - raw_verb: CREATE_VIEW
    primary_group: schema_create
    primary_subtype: create_view
    effects:
      - group: schema_create
        subtype: create_view
        objects:
          - { kind: view, name: v }
expected_decision_under_sample_policy:
  verb: deny
  rule_name: app-deny-mutations
