name: vacuum
spec_ref: §7.3 VACUUM, Appendix B maintenance
description: |
  VACUUM (no relations, no options) - bare maintenance command.
  Sample policy has no explicit maintenance rule; falls through to
  implicit deny (the safer default). RawVerb is VACUUM.
sql: "VACUUM"
expected_classification:
  - raw_verb: VACUUM
    primary_group: maintenance
    effects:
      - group: maintenance
expected_decision_under_sample_policy:
  verb: deny
