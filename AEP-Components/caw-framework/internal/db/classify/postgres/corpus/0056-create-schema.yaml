name: create-schema
spec_ref: §7.3 CREATE SCHEMA
description: CREATE SCHEMA maps to schema_create(create_schema).
sql: "CREATE SCHEMA app"
expected_classification:
  - raw_verb: CREATE_SCHEMA
    primary_group: schema_create
    primary_subtype: create_schema
    effects:
      - group: schema_create
        subtype: create_schema
        objects:
          - { kind: schema, name: app }
expected_decision_under_sample_policy:
  verb: deny
  rule_name: app-deny-mutations
