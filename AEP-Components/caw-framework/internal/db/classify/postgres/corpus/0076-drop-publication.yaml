name: drop-publication
spec_ref: §7.3 DROP PUBLICATION
description: DROP PUBLICATION → schema_destroy(drop_publication).
sql: "DROP PUBLICATION p"
expected_classification:
  - raw_verb: DROP_PUBLICATION
    primary_group: schema_destroy
    primary_subtype: drop_publication
    effects:
      - group: schema_destroy
        subtype: drop_publication
        objects:
          - { kind: publication, name: p }
expected_decision_under_sample_policy:
  verb: deny
  rule_name: app-deny-mutations
