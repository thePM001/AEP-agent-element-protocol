name: checkpoint
spec_ref: §7.3 CHECKPOINT, Appendix B maintenance
description: |
  CHECKPOINT - server-wide checkpoint, superuser-only. maintenance group.
  Sample policy: implicit deny.
sql: "CHECKPOINT"
expected_classification:
  - raw_verb: CHECKPOINT
    primary_group: maintenance
    effects:
      - group: maintenance
expected_decision_under_sample_policy:
  verb: deny
