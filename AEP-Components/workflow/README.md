# Workflow Phases

Multi-phase workflow executor and fine-tuning templates.

- **Component ID:** `workflow`
- **Path:** `workflow/`
- **Manifest:** `AEP-Base-Node/registry/components/workflow.json`

Runtime code lives in `lib/`.
