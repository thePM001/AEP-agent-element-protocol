[package]
name = "aep-agentmesh-local-issuance"
version = "0.1.0"
edition = "2021"
description = "AgentMesh docs must say local issuance. Do not sell zero-trust mesh attestation for a local cert."
license = "UNLICENSED"
publish = false

[[bin]]
name = "aep-agentmesh-local-issuance"
path = "src/main.rs"

[dependencies]
anyhow = "1"
