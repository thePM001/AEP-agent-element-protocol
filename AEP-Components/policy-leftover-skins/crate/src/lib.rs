// @PAD: gaplune-creation-pad via gaplune-pad-transform encode
// @GCDE: gaplune.policy.v1
// crate: aep-policy-leftover-skins
// Generated by GAPLUNE Creation PAD ( zero-LLM, Rust-only )
// tokens: 0
// GAP-285-P10 leftover yaml and rego files are not live Admit skins.

use std::fs;
use std::path::{Path, PathBuf};

pub const TICKET: &str = "GAP-285-P10";
pub const LEFTOVER_NAMING: &str =
    "Leftover yaml and rego files sit beside live GAP reference docs and are not live Admit skins.";
pub const COLLECT_ALL_ONLY_GAP: &str =
    "Collect-all loads only GAP reference docs under AEP-Policy-System/reference.";
pub const P6_POINTER: &str = "Operators use the reference GAP files from GAP-285-P6.";

pub const LEFTOVER_FILES: &[&str] = &[
    "aep-builder.policy.yaml",
    "coding-agent.policy.yaml",
    "content-safety.policy.yaml",
    "covenant-only.policy.yaml",
    "full-governance.policy.yaml",
    "multi-agent.policy.yaml",
    "network-egress-no-smtp.policy.yaml",
    "readonly-auditor.policy.yaml",
    "strict-production.policy.yaml",
    "aep-memory-policy.rego",
    "aep-policy.rego",
];

pub const P6_REFERENCE_GAPS: &[&str] = &[
    "writing.gap",
    "security.gap",
    "governance.gap",
    "deployment.gap",
    "network-egress-no-smtp.gap",
    "hipaa.gap",
    "gdpr.gap",
    "eu-ai-act.gap",
    "iso-42001.gap",
    "nist-ai-rmf.gap",
    "soc2-type2.gap",
];

pub fn leftover_suffix(name: &str) -> bool {
    name.ends_with(".policy.yaml") || name.ends_with(".rego")
}

pub fn names_leftover_not_admit(src: &str) -> bool {
    let l = src.to_ascii_lowercase();
    l.contains("leftover") && l.contains("not") && l.contains("live") && l.contains("admit")
}

pub fn leftover_not_admit(path: &Path) -> Result<String, String> {
    let name = path
        .file_name()
        .and_then(|s| s.to_str())
        .unwrap_or("");
    if leftover_suffix(name) {
        return Err(String::from(
            "leftover yaml or rego is not a live Admit skin",
        ));
    }
    Ok(String::from("ok not leftover"))
}

pub fn p6_reference_paths() -> Vec<String> {
    let mut out = Vec::new();
    let mut i = 0usize;
    while i < P6_REFERENCE_GAPS.len() {
        let mut s = String::from("AEP-Policy-System/reference/");
        s.push_str(P6_REFERENCE_GAPS[i]);
        out.push(s);
        i = i.saturating_add(1);
    }
    out
}

pub fn walk_to_workspace() -> PathBuf {
    let mut dir = if let Ok(m) = std::env::var("CARGO_MANIFEST_DIR") {
        PathBuf::from(m)
    } else {
        std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."))
    };
    let mut i = 0usize;
    while i < 16 {
        let probe = dir.join("AEP-Policy-System/reference/writing.gap");
        if probe.is_file() {
            return dir;
        }
        match dir.parent() {
            Some(parent) => dir = parent.to_path_buf(),
            None => break,
        }
        i = i.saturating_add(1);
    }
    PathBuf::from(".")
}

pub fn discover_policy_system_dir() -> Option<PathBuf> {
    if let Ok(p) = std::env::var("AEP_POLICY_SYSTEM_DIR") {
        let pb = PathBuf::from(p);
        if pb.is_dir() {
            return Some(pb);
        }
    }
    let root = walk_to_workspace();
    let dir = root.join("AEP-Policy-System");
    if dir.is_dir() {
        Some(dir)
    } else {
        None
    }
}

pub fn leftover_paths(dir: &Path) -> Vec<PathBuf> {
    let mut out = Vec::new();
    let mut i = 0usize;
    while i < LEFTOVER_FILES.len() {
        out.push(dir.join(LEFTOVER_FILES[i]));
        i = i.saturating_add(1);
    }
    out
}

pub fn collect_all_gap_paths(dir: &Path) -> Vec<PathBuf> {
    let mut out = Vec::new();
    let reference = dir.join("reference");
    if let Ok(rd) = fs::read_dir(&reference) {
        let mut files: Vec<PathBuf> = rd.filter_map(|e| e.ok().map(|e| e.path())).collect();
        files.sort();
        for pth in files {
            let name = pth
                .file_name()
                .and_then(|s| s.to_str())
                .unwrap_or("");
            if leftover_suffix(name) {
                continue;
            }
            if pth.extension().and_then(|e| e.to_str()) == Some("gap") {
                out.push(pth);
            }
        }
    }
    let lat = dir.join("lattice-channel-mandatory.gap");
    if lat.is_file() {
        out.push(lat);
    }
    out
}

pub fn scan_leftover_naming(dir: &Path) -> Result<String, String> {
    let mut i = 0usize;
    while i < LEFTOVER_FILES.len() {
        let name = LEFTOVER_FILES[i];
        let p = dir.join(name);
        let text = match fs::read_to_string(&p) {
            Ok(t) => t,
            Err(_) => {
                let mut e = String::from("missing leftover ");
                e.push_str(name);
                return Err(e);
            }
        };
        if names_leftover_not_admit(&text) == false {
            let mut e = String::from("leftover file ");
            e.push_str(name);
            e.push_str(" missing leftover naming");
            return Err(e);
        }
        match leftover_not_admit(&p) {
            Ok(_) => {
                return Err(String::from(
                    "leftover yaml or rego was treated as a live Admit skin",
                ))
            }
            Err(_) => {}
        }
        i = i.saturating_add(1);
    }
    Ok(String::from("ok leftover naming"))
}

pub fn scan_collect_all_gap_only(dir: &Path) -> Result<String, String> {
    let gaps = collect_all_gap_paths(dir);
    if gaps.is_empty() {
        return Err(String::from("collect-all found no GAP reference docs"));
    }
    let mut i = 0usize;
    while i < gaps.len() {
        let name = gaps[i]
            .file_name()
            .and_then(|s| s.to_str())
            .unwrap_or("");
        if leftover_suffix(name) {
            return Err(String::from(
                "collect-all loaded leftover yaml or rego as Admit",
            ));
        }
        let ext = gaps[i].extension().and_then(|e| e.to_str());
        if ext == Some("gap") {
        } else {
            return Err(String::from("collect-all loaded a non-GAP file"));
        }
        i = i.saturating_add(1);
    }
    Ok(String::from("ok collect-all loads only GAP reference docs"))
}

pub fn scan_p6_pointer() -> Result<String, String> {
    let refs = p6_reference_paths();
    if refs.len() == P6_REFERENCE_GAPS.len() {
    } else {
        return Err(String::from("P6 pointer list incomplete"));
    }
    let mut i = 0usize;
    while i < refs.len() {
        if refs[i].contains("AEP-Policy-System/reference/") == false {
            return Err(String::from("P6 pointer missed reference GAP path"));
        }
        i = i.saturating_add(1);
    }
    Ok(String::from(
        "ok operators use the reference GAP files from GAP-285-P6",
    ))
}

pub fn run_gate() -> Result<(), String> {
    let dir = match discover_policy_system_dir() {
        Some(d) => d,
        None => return Err(String::from("AEP-Policy-System missing")),
    };
    if let Err(e) = scan_leftover_naming(&dir) {
        return Err(e);
    }
    if let Err(e) = scan_collect_all_gap_only(&dir) {
        return Err(e);
    }
    if let Err(e) = scan_p6_pointer() {
        return Err(e);
    }
    Ok(())
}

/// HVVCAS leftover_inventory domain:admit type:service
pub mod leftover_inventory {
    use super::*;
    pub struct LeftoverInventory {
        pub policy_system_dir: String,
        pub leftover: Vec<String>,
    }
    impl LeftoverInventory {
        pub fn new() -> Self {
            Self {
                policy_system_dir: String::new(),
                leftover: Vec::new(),
            }
        }
        /// List leftover yaml and rego files beside live GAP reference docs
        pub fn process(&mut self) -> Result<(), String> {
            let dir = PathBuf::from(&self.policy_system_dir);
            let paths = leftover_paths(&dir);
            self.leftover.clear();
            let mut i = 0usize;
            while i < paths.len() {
                if let Some(s) = paths[i].to_str() {
                    self.leftover.push(String::from(s));
                }
                i = i.saturating_add(1);
            }
            Ok(())
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn leftover_suffix_yaml_and_rego() {
        assert ! (leftover_suffix("aep-builder.policy.yaml"));
        assert ! (leftover_suffix("aep-policy.rego"));
        assert ! (leftover_suffix("writing.gap") == false);
    }

    #[test]
    fn leftover_not_admit_denies_yaml_rego() {
        let y = PathBuf::from("aep-builder.policy.yaml");
        assert ! (leftover_not_admit(&y).is_err());
        let r = PathBuf::from("aep-policy.rego");
        assert ! (leftover_not_admit(&r).is_err());
        let g = PathBuf::from("writing.gap");
        assert ! (leftover_not_admit(&g).is_ok());
    }

    #[test]
    fn p6_pointer_names_reference_gaps() {
        scan_p6_pointer().expect("p6");
        let refs = p6_reference_paths();
        assert ! (refs.iter().any(|s| s.ends_with("writing.gap")));
        assert ! (refs.iter().any(|s| s.ends_with("security.gap")));
    }

    #[test]
    fn leftover_inventory_lists_ticket_scope() {
        let mut inv = leftover_inventory::LeftoverInventory::new();
        inv.policy_system_dir = String::from("/tmp/ps");
        inv.process().expect("inv");
        assert_eq ! (inv.leftover.len(), LEFTOVER_FILES.len());
    }

    #[test]
    fn live_tree_leftover_named_and_collect_all_gap_only() {
        let dir = discover_policy_system_dir().expect("policy system");
        scan_leftover_naming(&dir).expect("naming");
        scan_collect_all_gap_only(&dir).expect("collect-all");
        let gaps = collect_all_gap_paths(&dir);
        assert ! (gaps.iter().any(|p| p.file_name().unwrap() == "writing.gap"));
        assert ! (
            gaps.iter()
                .any(|p| p.file_name().and_then(|s| s.to_str()) == Some("aep-builder.policy.yaml"))
                == false
        );
    }

    #[test]
    fn naming_text_is_connected_prose() {
        assert ! (names_leftover_not_admit(LEFTOVER_NAMING));
        assert ! (COLLECT_ALL_ONLY_GAP.contains("Collect-all"));
        assert ! (P6_POINTER.contains("GAP-285-P6"));
    }
}
