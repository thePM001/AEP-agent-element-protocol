[package]
name = "aep-graph-engine-admit-gate"
version = "0.1.0"
edition = "2021"
description = "Fail if graph-engine engine.ts invokes nodeExecutor without admitGate. Default deny. Tick only after allow."
license = "UNLICENSED"
publish = false

[[bin]]
name = "aep-graph-engine-admit-gate"
path = "src/main.rs"

[dependencies]
anyhow = "1"
serde_json = "1"
