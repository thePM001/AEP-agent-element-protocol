# Agent Identity

Agent identity manager and compact identity types.

- **Component ID:** `identity`
- **Path:** `identity/`
- **Manifest:** `AEP-Base-Node/registry/components/identity.json`

Re-exported by `typescript-sdk/` for programmatic access. Runtime code lives in `lib/`.
