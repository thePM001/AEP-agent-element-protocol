# Session Governance

Agent session lifecycle, kill switch and session manager.

- **Component ID:** `session`
- **Path:** `session/`
- **Manifest:** `AEP-Base-Node/registry/components/session.json`

Runtime code lives in `lib/`.
