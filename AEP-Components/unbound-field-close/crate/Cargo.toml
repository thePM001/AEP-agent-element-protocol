[package]
name = "aep-unbound-field-close"
version = "0.1.0"
edition = "2021"
description = "Unbound scene, channel, time and sequence close. dest_dock may bind from the opened frame docking port. Do not reopen the earlier law change or the earlier law change."
license = "UNLICENSED"
publish = false

[[bin]]
name = "aep-unbound-field-close"
path = "src/main.rs"

[dependencies]
anyhow = "1"
serde_json = "1"
aep-envelope = { path = "../../envelope/crate" }
