# Lattice Memory

Attractor store for AEP 2.8 (sqlite-vec + USearch).

## Implementation

| Path | Crate |
|------|-------|
| `crate/` | `aep-lattice-memory` |

CLI binary: `aep-memory` (built from AEP-Base-Node crate workspace).