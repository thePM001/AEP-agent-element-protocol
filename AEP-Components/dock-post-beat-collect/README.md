# Dock post-beat collect

After the 1000 ms kernel pulse, the client asks for the applied result by capsule hash on the same dock. Enqueue is not Admit. TypeScript dynAEP does not own the wait. See `AEP-Base-Node/README.md` for how the compiled wait can be changed in theory.
