// @PAD: gaplune-creation-pad via gaplune-pad-transform encode
// @GCDE: gaplune-decode hmac-sha256:5b032c78617c9ca5a3f2a172bd7790a8f7639f1c4af21652fb9b48cd41b2f829
// crate: aep-admit-live-dock
// Generated by GAPLUNE Creation PAD ( zero-LLM )
// tokens: 0
// HVVCAS: live_collect_all domain:admit type:service
// AEP28-ENV-032: Product Admit wall crates fold into envelope_admit collect-all.
// AEP28-ENV-065: extra temporal walls measure 50 ms drift against freeze, not live now.
// AEP28-ENV-067: pass LatticeNode.wrap into policy-system compile.
use aep_admit::{admit_collect_all, AdmitWall, LatticeCompileInput};
use aep_admit_channel_order_walls::{
    compile_channel_walls, compile_partial_order_walls, ChannelCompileInput, PartialOrderCompileInput,
};
use aep_admit_temporal_bounds::{compile_temporal_walls, TemporalCompileInput};
use aep_admit_writing_walls::fold_writing_into_admit;
use aep_envelope_walls::compile_live_lattice_walls;
use aep_policy_system_admit::{compile_policy_system_walls, PolicySystemCompileInput};
use aep_live_entry::LiveEntry;
use serde_json::Value;
use std::fs;
use std::io::Write;
use std::path::{Path, PathBuf};

pub const TICKET: &str = "AEP28-ENV-032";
pub const TICKET_ENV065: &str = "AEP28-ENV-065";
pub const TICKET_ENV037: &str = "AEP28-ENV-037";

pub const PRODUCT_WALL_CRATES: &[&str] = &[
    "aep-admit",
    "aep-admit-writing-walls",
    "aep-admit-channel-order-walls",
    "aep-admit-temporal-bounds",
    "aep-envelope-walls",
    "aep-policy-system-admit",
];

pub const LIVE_PATH_CRATES: &[&str] = &[
    "aep-admit",
    "aep-admit-writing-walls",
    "aep-admit-channel-order-walls",
    "aep-admit-temporal-bounds",
    "aep-envelope-walls",
    "aep-envelope",
    "aep-live-entry",
    "aep-admit-live-dock",
    "aep-one-live-evaluation",
    "aep-policy-system-admit",
];

pub const CI_ONLY_ADMIT_CRATES: &[&str] = &[
    "aep-admit-parity",
    "aep-admit-opa-sole",
    "aep-process-event-admit-walls",
    "aep-admit-no-trust-tier",
];

pub const DROPPED_WALL_CRATES: &[&str] = &["aep-admit-trust-floor"];

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct LiveDockContext {
    pub channel_id: String,
    pub agent_id: String,
    pub session_id: String,
    pub docking_port: String,
    pub contract_id: String,
    pub has_capsule: bool,
    pub contract_active: bool,
    pub docking_wire: String,
    pub rate_limited: bool,
}

impl LiveDockContext {
    pub fn unit_open() -> Self {
        Self {
            channel_id: String::from("ex.dynaep.event"),
            agent_id: String::from("agent-a"),
            session_id: String::from("sess-unit"),
            docking_port: String::from("inference_engine"),
            contract_id: String::from("PM-DYNAEP-001"),
            has_capsule: true,
            contract_active: true,
            docking_wire: String::from("frame"),
            rate_limited: false,
        }
    }

    pub fn from_open_frame(
        channel_id: &str,
        agent_id: &str,
        session_id: &str,
        docking_port: &str,
        contract_id: &str,
    ) -> Self {
        Self {
            channel_id: String::from(channel_id),
            agent_id: String::from(agent_id),
            session_id: String::from(session_id),
            docking_port: String::from(docking_port),
            contract_id: String::from(contract_id),
            has_capsule: true,
            contract_active: true,
            docking_wire: String::from("frame"),
            rate_limited: false,
        }
    }
}

fn deny_prefix(reasons: &[String]) -> String {
    let mut s = String::from("Admit collect-all walls then Apply");
    if reasons.is_empty() == false {
        s.push_str(": ");
        s.push_str(&reasons.join("; "));
    }
    s
}

fn collect_strings(v: &Value, out: &mut Vec<String>) {
    match v {
        Value::String(s) => out.push(s.clone()),
        Value::Array(a) => {
            for x in a {
                collect_strings(x, out);
            }
        }
        Value::Object(m) => {
            for x in m.values() {
                collect_strings(x, out);
            }
        }
        _ => {}
    }
}

fn payload_empty(v: &Value) -> bool {
    match v.get("payload") {
        None => true,
        Some(Value::Null) => true,
        Some(Value::Object(m)) => m.is_empty(),
        Some(Value::Array(a)) => a.is_empty(),
        Some(Value::String(s)) => s.is_empty(),
        _ => false,
    }
}

fn channel_input(dock: &LiveDockContext) -> ChannelCompileInput {
    ChannelCompileInput {
        channel_id: dock.channel_id.clone(),
        agent_id: dock.agent_id.clone(),
        session_id: dock.session_id.clone(),
        docking_port: dock.docking_port.clone(),
        contract_id: dock.contract_id.clone(),
        has_capsule: dock.has_capsule,
        contract_active: dock.contract_active,
        docking_wire: dock.docking_wire.clone(),
        rate_limited: dock.rate_limited,
    }
}

/// Compile product Admit wall crates. This is not a combinator.
pub fn compile_live_walls(
    live: &LiveEntry,
    event: &Value,
    dock: &LiveDockContext,
) -> Vec<AdmitWall> {
    let action_path = event
        .get("action_path")
        .and_then(|v| v.as_str())
        .unwrap_or("")
        .to_string();
    let event_agent = event
        .get("agent_id")
        .and_then(|v| v.as_str())
        .unwrap_or("")
        .to_string();
    let agent_id = if event_agent.is_empty() {
        dock.agent_id.clone()
    } else {
        event_agent
    };
    let timestamp = event.get("timestamp").and_then(|v| v.as_i64()).unwrap_or(0);
    // AEP28-ENV-065: extra temporal walls measure 50 ms drift against freeze, not live now.
    let bridge = live.wall_bridge_ts_ms();
    let node = live.snapshot.lattice_nodes.get(&action_path);
    let category = node.map(|n| n.category.clone()).unwrap_or_default();
    let parents = node.map(|n| n.parents.clone()).unwrap_or_default();
    let agent_may = node.map(|n| n.agent_may.clone()).unwrap_or_default();
    let wrap = node.map(|n| n.wrap.clone()).unwrap_or_default();
    let all_actions: Vec<String> = live.snapshot.lattice_nodes.keys().cloned().collect();
    let satisfied: Vec<String> = live.snapshot.satisfied_actions.iter().cloned().collect();

    let mut strings = Vec::new();
    collect_strings(event, &mut strings);
    let writing_text = strings.join("\n");
    let writing_walls = aep_admit::compile_writing_walls(&writing_text);
    let folded = fold_writing_into_admit(&writing_text, &[]);
    let _ = aep_admit_writing_walls::writing_wall_id(aep_admit::RULE_NO_EM_DASHES);

    let mut walls: Vec<AdmitWall> = writing_walls;
    if folded.allow == false {
        for w in folded.closed {
            if walls
                .iter()
                .any(|x| x.id == w.id && x.reason == w.reason)
                == false
            {
                walls.push(w);
            }
        }
    }

    walls.extend(compile_channel_walls(&channel_input(dock)));
    let order = PartialOrderCompileInput {
        action_path: action_path.clone(),
        parents: parents.clone(),
        satisfied: satisfied.clone(),
        agent_id: agent_id.clone(),
    };
    walls.extend(compile_partial_order_walls(&order));

    let mut temporal = TemporalCompileInput::with_defaults();
    temporal.has_agent_time = timestamp != 0;
    temporal.agent_time_ms = timestamp;
    temporal.bridge_time_ms = bridge;
    temporal.max_drift_ms = live.snapshot.max_drift_ms;
    temporal.max_future_ms = live.snapshot.max_future_ms;
    temporal.max_staleness_ms = live.snapshot.max_age_ms;
    temporal.agent_id = agent_id.clone();
    walls.extend(compile_temporal_walls(&temporal));

    let mut lattice_in = LatticeCompileInput::default();
    lattice_in.action_path = action_path;
    lattice_in.category = category;
    lattice_in.agent_id = agent_id;
    lattice_in.agent_may = agent_may;
    lattice_in.satisfied_actions = satisfied;
    lattice_in.parents_of = parents.clone();
    lattice_in.is_root = parents.is_empty();
    lattice_in.all_actions = all_actions;
    lattice_in.simultaneous_outputs = live.snapshot.simultaneous_outputs;
    lattice_in.event_rate = live.snapshot.event_rate as f64;
    lattice_in.payload_empty = payload_empty(event);
    lattice_in.payload_repeated_violation = false;
    walls.extend(compile_live_lattice_walls(&lattice_in));
    let mut policy_in = PolicySystemCompileInput::default();
    policy_in.text = writing_text;
    policy_in.action_path = lattice_in.action_path.clone();
    policy_in.docking_wire = dock.docking_wire.clone();
    policy_in.docking_port = dock.docking_port.clone();
    policy_in.event_type = event
        .get("type")
        .and_then(|v| v.as_str())
        .unwrap_or("")
        .to_string();
    policy_in.wrap = wrap;
    policy_in.event = event.clone();
    walls.extend(compile_policy_system_walls(&policy_in));
    walls
}

/// Fold every product Admit wall crate into one live collect-all pass.
/// Product live dock does not call this beside process_event Admit. See AEP28-ENV-037.
pub fn live_collect_all(
    live: &LiveEntry,
    event: &Value,
    dock: &LiveDockContext,
) -> Result<(), String> {
    let walls = compile_live_walls(live, event, dock);
    let result = admit_collect_all(&walls);
    if result.allow {
        Ok(())
    } else {
        let reasons: Vec<String> = result.closed.iter().map(|w| w.reason.clone()).collect();
        Err(deny_prefix(&reasons))
    }
}

fn quoted_strings(text: &str) -> Vec<String> {
    let mut out = Vec::new();
    let bytes = text.as_bytes();
    let mut i = 0usize;
    while i < bytes.len() {
        if bytes[i] == b'"' {
            i = i.saturating_add(1);
            let start = i;
            while i < bytes.len() && bytes[i] != b'"' {
                i = i.saturating_add(1);
            }
            if i <= bytes.len() && start <= i {
                if let Ok(s) = std::str::from_utf8(&bytes[start..i]) {
                    if s.is_empty() == false {
                        out.push(s.to_string());
                    }
                }
            }
            i = i.saturating_add(1);
        } else {
            i = i.saturating_add(1);
        }
    }
    out
}

fn package_name(cargo: &str) -> String {
    let mut in_pkg = false;
    for line in cargo.lines() {
        let t = line.trim();
        if t == "[package]" {
            in_pkg = true;
            continue;
        }
        if t.starts_with('[') {
            in_pkg = false;
            continue;
        }
        if in_pkg && t.starts_with("name") {
            if let Some(q) = t.split('"').nth(1) {
                return q.to_string();
            }
        }
    }
    String::new()
}

fn dep_names(cargo: &str) -> Vec<String> {
    let mut in_deps = false;
    let mut out = Vec::new();
    for line in cargo.lines() {
        let t = line.trim();
        if t == "[dependencies]" || t == "[dev-dependencies]" {
            in_deps = true;
            continue;
        }
        if t.starts_with('[') {
            in_deps = false;
            continue;
        }
        if in_deps == false || t.is_empty() || t.starts_with('#') {
            continue;
        }
        if let Some((k, _)) = t.split_once('=') {
            let name = k.trim();
            if name.starts_with("aep-") {
                out.push(name.to_string());
            }
        }
    }
    out
}

fn workspace_members(cargo: &str) -> Vec<String> {
    let mut out = Vec::new();
    let mut in_members = false;
    for line in cargo.lines() {
        let t = line.trim();
        if t.starts_with("members") && t.contains('[') {
            in_members = true;
        }
        if in_members {
            out.extend(quoted_strings(t).into_iter().filter(|s| s.contains('/')));
            if t.contains(']') {
                in_members = false;
            }
        }
    }
    out
}

fn set_has(items: &[String], needle: &str) -> bool {
    items.iter().any(|s| s == needle)
}

pub fn walk_to_workspace() -> PathBuf {
    let mut dir = if let Ok(m) = std::env::var("CARGO_MANIFEST_DIR") {
        PathBuf::from(m)
    } else {
        std::env::current_dir().unwrap_or_else(|_| PathBuf::from("."))
    };
    let mut i = 0usize;
    while i < 10 {
        let cargo = dir.join("Cargo.toml");
        let admit = dir.join("AEP-Components/admit/crate/Cargo.toml");
        if cargo.is_file() && admit.is_file() {
            return dir;
        }
        match dir.parent() {
            Some(parent) => dir = parent.to_path_buf(),
            None => break,
        }
        i = i.saturating_add(1);
    }
    PathBuf::from(".")
}

fn read_text(path: &Path) -> String {
    fs::read_to_string(path).unwrap_or_default()
}

fn classify_admit_package(name: &str) -> &'static str {
    if name == "aep-admit-live-dock" {
        return "live-dock";
    }
    if PRODUCT_WALL_CRATES.iter().any(|c| *c == name) {
        return "wall";
    }
    if CI_ONLY_ADMIT_CRATES.iter().any(|c| *c == name) {
        return "ci-only";
    }
    if DROPPED_WALL_CRATES.iter().any(|c| *c == name) {
        return "dropped";
    }
    "unknown"
}

fn extract_after(src: &str, needle: &str) -> String {
    let start = match src.find(needle) {
        Some(v) => v,
        None => return String::new(),
    };
    let rest = &src[start..];
    let brace = match rest.find("{") {
        Some(v) => v,
        None => return String::new(),
    };
    let bytes = rest.as_bytes();
    let mut i = brace;
    let mut depth: i32 = 0;
    while i < bytes.len() {
        if bytes[i] == b"{"[0] {
            depth += 1;
        } else if bytes[i] == b"}"[0] {
            depth -= 1;
            if depth == 0 {
                return rest[..=i].to_string();
            }
        }
        i += 1;
    }
    String::new()
}

fn strip_line_comments(src: &str) -> String {
    let mut out = String::new();
    for line in src.lines() {
        if let Some(i) = line.find("//") {
            out.push_str(&line[..i]);
        } else {
            out.push_str(line);
        }
        out.push('\n');
    }
    out
}

fn compact_src(src: &str) -> String {
    src.chars().filter(|c| c.is_whitespace() == false).collect()
}

/// CI: envelope_admit is one Admit then Apply. Not live_collect_all beside process_event.
pub fn scan_envelope_admit_one_live_evaluation(src: &str) -> Result<String, String> {
    if src.contains("aep_admit_live_dock") == false {
        return Err(String::from("envelope_admit missing aep_admit_live_dock"));
    }
    if src.contains("aep_one_live_evaluation") == false {
        return Err(String::from("envelope_admit missing aep_one_live_evaluation"));
    }
    if src.contains("attach_live_walls") == false {
        return Err(String::from("envelope_admit missing attach_live_walls"));
    }
    if src.contains("process_event") == false {
        return Err(String::from("envelope_admit missing process_event"));
    }
    let body = extract_after(src, "fn admit_sealed_payload_on_live_dock");
    if body.is_empty() {
        return Err(String::from("admit_sealed_payload_on_live_dock not found"));
    }
    let code = compact_src(&strip_line_comments(&body));
    if code.contains("live_collect_all") && code.contains("process_event") {
        return Err(String::from(
            "envelope_admit calls both live_collect_all and process_event Admit on the same plaintext",
        ));
    }
    Ok(String::from("ok one live evaluation"))
}
/// Fail if a product Admit wall crate is a workspace member and not on the live dock path.
pub fn live_path_wall_member_gate(root: &Path) -> Result<String, String> {
    let ws = read_text(&root.join("Cargo.toml"));
    if ws.is_empty() {
        return Err(String::from("workspace Cargo.toml missing"));
    }
    let members = workspace_members(&ws);
    let mut member_names: Vec<String> = Vec::new();
    for rel in &members {
        let cargo = root.join(rel).join("Cargo.toml");
        let name = package_name(&read_text(&cargo));
        if name.is_empty() == false {
            member_names.push(name);
        }
    }
    for dropped in DROPPED_WALL_CRATES {
        if member_names.iter().any(|n| n == dropped) {
            let mut msg = String::from("dropped wall crate still a workspace member: ");
            msg.push_str(dropped);
            return Err(msg);
        }
        for rel in &members {
            if rel.contains("admit-trust-floor") {
                return Err(String::from(
                    "admit-trust-floor remains a workspace product member",
                ));
            }
        }
    }
    let live_dock_cargo = read_text(&root.join("AEP-Components/admit-live-dock/crate/Cargo.toml"));
    let live_dock_deps = dep_names(&live_dock_cargo);
    let base_deps = dep_names(&read_text(&root.join("AEP-Base-Node/crate/Cargo.toml")));
    let live_entry_deps = dep_names(&read_text(
        &root.join("AEP-Components/live-entry/crate/Cargo.toml"),
    ));
    let mut union = live_dock_deps.clone();
    union.extend(base_deps);
    union.extend(live_entry_deps);
    union.push(String::from("aep-admit-live-dock"));
    union.sort();
    union.dedup();

    for wall in PRODUCT_WALL_CRATES {
        if member_names.iter().any(|n| n == wall) == false {
            let mut msg = String::from("product wall crate missing from workspace: ");
            msg.push_str(wall);
            return Err(msg);
        }
        if set_has(&live_dock_deps, wall) == false {
            let mut msg = String::from("product wall crate not imported on live dock: ");
            msg.push_str(wall);
            return Err(msg);
        }
    }
    for live in LIVE_PATH_CRATES {
        if set_has(&union, live) == false {
            let mut msg = String::from("live-path crate missing from dock union: ");
            msg.push_str(live);
            return Err(msg);
        }
    }
    for name in &member_names {
        if name.starts_with("aep-admit") == false && name != "aep-envelope-walls" {
            continue;
        }
        match classify_admit_package(name) {
            "wall" | "ci-only" | "live-dock" => {}
            "dropped" => {
                let mut msg = String::from("dropped wall crate still a workspace member: ");
                msg.push_str(name);
                return Err(msg);
            }
            _ => {
                let mut msg = String::from("unused product wall crate: ");
                msg.push_str(name);
                return Err(msg);
            }
        }
    }
    let admit_src = read_text(&root.join("AEP-Base-Node/crate/src/envelope_admit.rs"));
    scan_envelope_admit_one_live_evaluation(&admit_src)?;
    let base_cargo = dep_names(&read_text(&root.join("AEP-Base-Node/crate/Cargo.toml")));
    if set_has(&base_cargo, "aep-admit-live-dock") == false {
        return Err(String::from("aep-base-node missing aep-admit-live-dock"));
    }
    if set_has(&base_cargo, "aep-one-live-evaluation") == false {
        return Err(String::from("aep-base-node missing aep-one-live-evaluation"));
    }
    let mut proof = String::from("ok live-path=");
    proof.push_str(&LIVE_PATH_CRATES.join(","));
    Ok(proof)
}

pub fn run_gate() -> Result<i32, String> {
    let root = walk_to_workspace();
    let proof = live_path_wall_member_gate(&root)?;
    let mut line = String::from("aep-admit-live-dock ");
    line.push_str(&proof);
    line.push('\n');
    let _ = std::io::stdout().write_all(line.as_bytes());
    Ok(0)
}

// @PAD: gaplune-creation-pad emit ( zero-LLM )
// HVVCAS: live_collect_all domain:admit type:service
pub mod live_collect_all {
    use super::{live_collect_all, LiveDockContext, LiveEntry};
    use serde_json::Value;

    pub struct LiveCollectAll {
        pub event: String,
        pub result: String,
    }

    impl LiveCollectAll {
        pub fn new() -> Self {
            Self {
                event: String::new(),
                result: String::new(),
            }
        }

        /// Fold product Admit wall crates into one live collect-all pass
        pub fn process(&mut self) -> anyhow::Result<()> {
            let value: Value = match serde_json::from_str(&self.event) {
                Ok(v) => v,
                Err(_) => Value::Null,
            };
            let live = LiveEntry::new();
            match live_collect_all(&live, &value, &LiveDockContext::unit_open()) {
                Ok(()) => self.result = String::from("allow=true"),
                Err(e) => self.result = e,
            }
            Ok(())
        }
    }
}

#[cfg(test)]
mod tests {
    use super::*;
    use aep_admit::writing_wall_id;

    fn yaml() -> &'static str {
        "actions:\n  root:ping:\n    category: system_event\n    parents: []\n    children: []\n    agent_may: []\n  action:write:\n    category: agent_action\n    parents: [\"root:ping\"]\n    children: []\n    agent_may: [\"agent-a\"]\n"
    }

    fn must(cond: bool) {
        if cond == false {
            std::process::abort();
        }
    }

    #[test]
    fn ticket_id() {
        must(TICKET == "AEP28-ENV-032");
    }

    #[test]
    fn unit_open_allows_known_ping() {
        let mut live = LiveEntry::from_yaml(yaml()).expect("yaml");
        live.set_clock_ms(1000000);
        let event: Value = serde_json::from_str(
            "{\"type\":\"PING\",\"action_path\":\"root:ping\",\"payload\":{\"ok\":true},\"timestamp\":1000000,\"target_id\":\"scene-a\",\"dest_dock\":\"inference_engine\",\"_sequenceNumber\":1}",
        )
        .unwrap();
        must(live_collect_all(&live, &event, &LiveDockContext::unit_open()).is_ok());
    }

    #[test]
    fn unknown_path_denies() {
        let mut live = LiveEntry::from_yaml(yaml()).expect("yaml");
        live.set_clock_ms(1000000);
        let event: Value = serde_json::from_str(
            "{\"type\":\"CUSTOM\",\"action_path\":\"bogus:path\",\"payload\":{\"ok\":true},\"timestamp\":1000000}",
        )
        .unwrap();
        let err = live_collect_all(&live, &event, &LiveDockContext::unit_open()).expect_err("deny");
        must(err.contains("Admit collect-all walls then Apply"));
    }

    #[test]
    fn writing_wall_closes_on_live_pass() {
        let mut live = LiveEntry::from_yaml(yaml()).expect("yaml");
        live.set_clock_ms(1000000);
        let mut body = String::from(
            "{\"type\":\"PING\",\"action_path\":\"root:ping\",\"payload\":{\"note\":\"bad",
        );
        body.push('\u{2014}');
        body.push_str("dash\"},\"timestamp\":1000000}");
        let event: Value = serde_json::from_str(&body).unwrap();
        let err = live_collect_all(&live, &event, &LiveDockContext::unit_open()).expect_err("deny");
        must(err.contains("Admit collect-all walls then Apply"));
        let _ = writing_wall_id(aep_admit::RULE_NO_EM_DASHES);
    }

    #[test]
    fn missing_capsule_closes_channel_wall() {
        let mut live = LiveEntry::from_yaml(yaml()).expect("yaml");
        live.set_clock_ms(1000000);
        let mut dock = LiveDockContext::unit_open();
        dock.has_capsule = false;
        dock.docking_wire = String::from("ping");
        dock.channel_id = String::new();
        let event: Value = serde_json::from_str(
            "{\"type\":\"PING\",\"action_path\":\"root:ping\",\"payload\":{\"ok\":true},\"timestamp\":1000000,\"target_id\":\"scene-a\",\"dest_dock\":\"inference_engine\",\"_sequenceNumber\":1}",
        )
        .unwrap();
        let err = live_collect_all(&live, &event, &dock).expect_err("deny");
        must(err.contains("Admit collect-all walls then Apply"));
    }

    #[test]
    fn known_write_allows_when_parent_satisfied() {
        let mut live = LiveEntry::from_yaml(yaml()).expect("yaml");
        live.set_clock_ms(1000000);
        live.snapshot
            .satisfied_actions
            .insert(String::from("root:ping"));
        let event: Value = serde_json::from_str(
            "{\"type\":\"CUSTOM\",\"action_path\":\"action:write\",\"agent_id\":\"agent-a\",\"payload\":{\"ok\":true},\"timestamp\":1000000,\"target_id\":\"scene-a\",\"dest_dock\":\"inference_engine\",\"_sequenceNumber\":1}",
        )
        .unwrap();
        must(live_collect_all(&live, &event, &LiveDockContext::unit_open()).is_ok());
    }

    #[test]
    fn hvvc_as_process_collects() {
        let mut svc = live_collect_all::LiveCollectAll::new();
        svc.event = String::from("{\"type\":\"PING\"}");
        svc.process().expect("process");
        must(svc.result.contains("Admit collect-all walls then Apply"));
    }

    #[test]
    fn unused_gate_fails_trust_floor_member() {
        let tmp = std::env::temp_dir().join("aep28-env-032-trust-floor");
        let _ = fs::remove_dir_all(&tmp);
        fs::create_dir_all(tmp.join("AEP-Components/admit/crate")).expect("mkdir");
        fs::write(
            tmp.join("Cargo.toml"),
            "[workspace]\nmembers = [\n    \"AEP-Components/admit-trust-floor/crate\",\n]\n",
        )
        .expect("ws");
        fs::create_dir_all(tmp.join("AEP-Components/admit-trust-floor/crate")).expect("mkdir");
        fs::write(
            tmp.join("AEP-Components/admit-trust-floor/crate/Cargo.toml"),
            "[package]\nname = \"aep-admit-trust-floor\"\nversion = \"0.1.0\"\n",
        )
        .expect("pkg");
        let err = live_path_wall_member_gate(&tmp).expect_err("deny");
        must(err.contains("admit-trust-floor") || err.contains("dropped"));
        let _ = fs::remove_dir_all(&tmp);
    }

    #[test]
    fn unused_gate_fails_wall_not_on_live_dock() {
        let tmp = std::env::temp_dir().join("aep28-env-032-missing-dep");
        let _ = fs::remove_dir_all(&tmp);
        fs::create_dir_all(tmp.join("AEP-Components/admit/crate")).expect("mkdir");
        fs::create_dir_all(tmp.join("AEP-Components/admit-writing-walls/crate")).expect("mkdir");
        fs::create_dir_all(tmp.join("AEP-Components/admit-channel-order-walls/crate")).expect("mkdir");
        fs::create_dir_all(tmp.join("AEP-Components/admit-temporal-bounds/crate")).expect("mkdir");
        fs::create_dir_all(tmp.join("AEP-Components/envelope-walls/crate")).expect("mkdir");
        fs::create_dir_all(tmp.join("AEP-Components/admit-live-dock/crate")).expect("mkdir");
        fs::create_dir_all(tmp.join("AEP-Base-Node/crate/src")).expect("mkdir");
        fs::create_dir_all(tmp.join("AEP-Components/live-entry/crate")).expect("mkdir");
        fs::write(
            tmp.join("Cargo.toml"),
            "[workspace]\nmembers = [\n    \"AEP-Components/admit/crate\",\n    \"AEP-Components/admit-writing-walls/crate\",\n    \"AEP-Components/admit-channel-order-walls/crate\",\n    \"AEP-Components/admit-temporal-bounds/crate\",\n    \"AEP-Components/envelope-walls/crate\",\n    \"AEP-Components/admit-live-dock/crate\",\n]\n",
        )
        .expect("ws");
        fs::write(
            tmp.join("AEP-Components/admit/crate/Cargo.toml"),
            "[package]\nname = \"aep-admit\"\nversion = \"0.1.0\"\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Components/admit-writing-walls/crate/Cargo.toml"),
            "[package]\nname = \"aep-admit-writing-walls\"\nversion = \"0.1.0\"\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Components/admit-channel-order-walls/crate/Cargo.toml"),
            "[package]\nname = \"aep-admit-channel-order-walls\"\nversion = \"0.1.0\"\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Components/admit-temporal-bounds/crate/Cargo.toml"),
            "[package]\nname = \"aep-admit-temporal-bounds\"\nversion = \"0.1.0\"\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Components/envelope-walls/crate/Cargo.toml"),
            "[package]\nname = \"aep-envelope-walls\"\nversion = \"0.1.0\"\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Components/admit-live-dock/crate/Cargo.toml"),
            "[package]\nname = \"aep-admit-live-dock\"\nversion = \"0.1.0\"\n[dependencies]\naep-admit = { path = \"../admit/crate\" }\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Base-Node/crate/Cargo.toml"),
            "[package]\nname = \"aep-base-node\"\nversion = \"0.1.0\"\n[dependencies]\naep-admit-live-dock = { path = \"x\" }\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Components/live-entry/crate/Cargo.toml"),
            "[package]\nname = \"aep-live-entry\"\nversion = \"0.1.0\"\n[dependencies]\naep-envelope = { path = \"x\" }\n",
        )
        .expect("pkg");
        fs::write(
            tmp.join("AEP-Base-Node/crate/src/envelope_admit.rs"),
            "use aep_admit_live_dock::live_collect_all;\n",
        )
        .expect("src");
        let err = live_path_wall_member_gate(&tmp).expect_err("deny");
        must(err.contains("not imported on live dock"));
        let _ = fs::remove_dir_all(&tmp);
    }

    #[test]
    fn live_workspace_unused_wall_gate() {
        let root = walk_to_workspace();
        match live_path_wall_member_gate(&root) {
            Ok(proof) => must(proof.contains("ok live-path=")),
            Err(e) => {
                let _ = std::io::stderr().write_all(e.as_bytes());
                std::process::abort();
            }
        }
    }

    #[test]
    fn env037_ticket() {
        must(TICKET_ENV037 == "AEP28-ENV-037");
    }

    #[test]
    fn compile_live_walls_returns_walls() {
        let mut live = LiveEntry::from_yaml(yaml()).expect("yaml");
        live.set_clock_ms(1000000);
        let event = serde_json::json!({
            "type": "PING",
            "action_path": "root:ping",
            "payload": { "ok": true },
            "timestamp": 1000000
        });
        let walls = compile_live_walls(&live, &event, &LiveDockContext::unit_open());
        must(walls.is_empty() == false);
    }

    #[test]
    fn compile_live_walls_drift_against_freeze() {
        must(TICKET_ENV065 == "AEP28-ENV-065");
        let mut live = LiveEntry::from_yaml(yaml()).expect("yaml");
        live.set_clock_ms(1000000);
        live.freeze_temporal_snapshot(1000000);
        live.set_clock_ms(1001000);
        let event = serde_json::json!({"type":"PING","action_path":"root:ping","payload":{"ok":true},"timestamp":1000040,"target_id":"scene-a","_sequenceNumber":1});
        let walls = compile_live_walls(&live, &event, &LiveDockContext::unit_open());
        let mut saw = false;
        for w in &walls {
            if w.id == "temporal:drift_exceeded" { saw = true; must(w.closed == false); }
            if w.id == "temporal:stale_event" { must(w.closed == false); }
        }
        must(saw);
        let compact: String = include_str!("lib.rs").chars().filter(|c| c.is_whitespace() == false).collect();
        must(compact.contains("live.wall_bridge_ts_ms()"));
        let bad = ["letbridge=live.now_ms", "()"].concat();
        must(compact.contains(&bad) == false);
        let mut live2 = LiveEntry::from_yaml(yaml()).expect("yaml");
        live2.set_clock_ms(1001000);
        let event2 = serde_json::json!({"type":"PING","action_path":"root:ping","payload":{"ok":true},"timestamp":1000000,"target_id":"scene-a","_sequenceNumber":1});
        let walls2 = compile_live_walls(&live2, &event2, &LiveDockContext::unit_open());
        must(walls2.iter().any(|w| w.id == "temporal:drift_exceeded" && w.closed));
    }

    #[test]
    fn dual_combinator_scan_fails() {
        let bad = "use aep_admit_live_dock::LiveDockContext; use aep_one_live_evaluation::attach_live_walls; pub fn admit_sealed_payload_on_live_dock() { live_collect_all(live, &value, dock)?; match live.process_event(value) { _ => {} } }";
        match scan_envelope_admit_one_live_evaluation(bad) {
            Err(e) => must(e.contains("both live_collect_all and process_event")),
            Ok(_) => std::process::abort(),
        }
    }

    #[test]
    fn one_admit_scan_passes() {
        let good = "use aep_admit_live_dock::LiveDockContext; use aep_one_live_evaluation::attach_live_walls; pub fn admit_sealed_payload_on_live_dock() { attach_live_walls(live, &value, dock); match live.process_event(value) { _ => {} } }";
        match scan_envelope_admit_one_live_evaluation(good) {
            Ok(v) => must(v.contains("ok one live evaluation")),
            Err(_) => std::process::abort(),
        }
    }
}
