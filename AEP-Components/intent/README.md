# Intent Drift

Intent baseline and drift detection.

- **Component ID:** `intent`
- **Path:** `intent/`
- **Manifest:** `AEP-Base-Node/registry/components/intent.json`

Re-exported by `typescript-sdk/` for programmatic access. Runtime code lives in `lib/`.
